{
  "configurations": [
    {
      "name": "Attach to Remote",
      "type": "go",
      "request": "attach",
      "mode": "remote",
      "port": 9002,
      "cwd": "${workspaceFolder}"
    }
  ]
}
